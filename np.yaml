apiVersion: v1
kind: Service
metadata:
  name: npsvc-nilesh
  namespace: nilesh
spec:
  type: NodePort
  selector:
    app: nginx
  ports:
  - port: 80  #service port inside cluster
    targetPort: 80 #container port
    nodePort: 30021